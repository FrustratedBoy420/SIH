# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Build and verification output. `refshots/` is tracked on purpose -- those are
# the reference images `verify.mjs` compares against, so they are source, not
# output. `shots/` is what a run produces and is ignored at the repository root.
.vite/
test-results/
playwright-report/

