# Rule for agents

Work on `main` only. Never create, check out, or commit to any other branch.
Pull (`git pull --rebase`) before starting; push to `origin main`.
See the "Rule: `main` only" section of README.md.
