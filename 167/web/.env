# Which engine answers queries: preview | http | auto
#   preview  the in-browser port of the classical pipeline (default while the API is not running)
#   http     the SatQuery API on the same origin
#   auto     probe /api/health once and pick
# Switch to `auto` once the API serves /api/health. Overridable per visit with ?engine=
VITE_ENGINE=preview
